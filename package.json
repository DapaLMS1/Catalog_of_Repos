{
  "name": "repo-preview-generator",
  "version": "1.0.0",
  "description": "Dependencies for generating social media preview cards using Node.js.",
  "main": "generate_preview_script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^22.0.0"
  }
}
